import React from 'react';

import {
  SafeAreaView,
  ScrollView,
  StatusBar,
  StyleSheet,
  Text,
  useColorScheme,
  View,
} from 'react-native';


const Cart = () =>{

    return(
          <SafeAreaView style={{flex: 1}}>
              <Text>Cart</Text>
            </SafeAreaView>
    );
}

export default Cart;